<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.from.mapper.BookReviewMapper">

    <insert id="insertReview" parameterType="BookReview" useGeneratedKeys="true" keyProperty="reviewId">
        INSERT INTO book_reviews (
            user_id, book_id, paper_id, emphasis_content, tone,
            delivery_date, delivery_time, ai_content, generation_status, is_sent, created_at, updated_at
        ) VALUES (
                     #{userId}, #{bookId}, #{paperId}, #{emphasisContent}, #{tone},
                     #{deliveryDate}, #{deliveryTime}, #{aiContent}, 'COMPLETED', 0, NOW(), NOW()
                 )
    </insert>

    <select id="findById" parameterType="Long" resultType="BookReview">
        SELECT * FROM book_reviews WHERE review_id = #{reviewId}
    </select>

    <!-- 발송 시간이 된 미발송 독후감 조회 -->
    <select id="findPendingReviews" resultType="BookReview">
        SELECT * FROM book_reviews
        WHERE is_sent = 0
          AND generation_status = 'COMPLETED'
          AND CONCAT(delivery_date, ' ', delivery_time) &lt;= NOW()
    </select>

    <update id="markAsSent" parameterType="Long">
        UPDATE book_reviews
        SET is_sent = 1, updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

    <select id="findByUserId" parameterType="long" resultType="BookReview">
        SELECT br.review_id, br.user_id, br.book_id, br.paper_id,
               br.emphasis_content, br.tone, br.delivery_day,
               br.delivery_date, br.delivery_time,
               br.is_sent, br.ai_content, br.generation_status,
               br.created_at, br.updated_at,
               b.title AS bookTitle, b.author AS bookAuthor
        FROM book_reviews br
                 JOIN books b ON br.book_id = b.book_id
        WHERE br.user_id = #{userId}
        ORDER BY br.created_at DESC
    </select>

</mapper>